# 🔄 Диаграмма рабочего процесса запуска на кластере

## Схема полного процесса

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        ВАШ ЛОКАЛЬНЫЙ КОМПЬЮТЕР                          │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ 1. Сборка Docker образа
                                    ▼
                    ┌─────────────────────────────┐
                    │   docker build/buildx       │
                    │   ./build_and_push.sh       │
                    └─────────────────────────────┘
                                    │
                                    │ 2. Публикация в Docker Hub
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                            DOCKER HUB                                   │
│                  YOUR_USERNAME/llabs_lda_hyperopt:v1                    │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ 3. Скачивание и конвертация
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    ВИРТУАЛКА С ENROOT (188.44.41.125)                   │
│                       sudo enroot import docker://...                   │
│                  Output: llabs_lda_hyperopt.sqsh                        │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ 4. Скачивание .sqsh через scp
                                    ▼
                    ┌─────────────────────────────┐
                    │  ВАШ ЛОКАЛЬНЫЙ КОМПЬЮТЕР    │
                    │  .sqsh файл на диске        │
                    └─────────────────────────────┘
                                    │
                                    │ 5. Загрузка на slurm-master
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    SLURM MASTER (10.36.60.202)                          │
│                  /scratch/$USER/llabs_lda_hyperopt.sqsh                 │
│                  /scratch/$USER/run.sh                                  │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ 6. Запуск задачи
                                    │    sbatch run.sh
                                    ▼
                    ┌─────────────────────────────┐
                    │     SLURM SCHEDULER         │
                    │   (очередь задач)           │
                    └─────────────────────────────┘
                                    │
                                    │ 7. Выделение ресурсов
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        COMPUTE NODE (Worker)                            │
│                                                                         │
│  ┌────────────────────────────────────────────────────────────┐        │
│  │              CONTAINER (llabs_lda_hyperopt.sqsh)           │        │
│  │                                                             │        │
│  │  ┌──────────────────────────────────────────────────────┐  │        │
│  │  │            for_klaster.py                            │  │        │
│  │  │                                                       │  │        │
│  │  │  Stage 1: Train PABBO Small                          │  │        │
│  │  │  Stage 2: Evaluate PABBO Small                       │  │        │
│  │  │  Stage 3: Train PABBO Large                          │  │        │
│  │  │  Stage 4: Evaluate PABBO Large                       │  │        │
│  │  │                                                       │  │        │
│  │  │  Stage 5: Run LDA Experiments (4 CORES)              │  │        │
│  │  │  ┌─────────┬─────────┬─────────┬─────────┐          │  │        │
│  │  │  │ Proc 1  │ Proc 2  │ Proc 3  │ Proc 4  │          │  │        │
│  │  │  │   GA    │   ES    │ PABBO_S │ PABBO_L │          │  │        │
│  │  │  │  (4 th) │  (4 th) │  (4 th) │  (4 th) │          │  │        │
│  │  │  └─────────┴─────────┴─────────┴─────────┘          │  │        │
│  │  │                                                       │  │        │
│  │  │  Stage 6: Aggregate Results                          │  │        │
│  │  └──────────────────────────────────────────────────────┘  │        │
│  │                                                             │        │
│  │  Монтированные директории:                                 │        │
│  │  /app/lda_pipeline_results/ ──► /scratch/$USER/lda_results │        │
│  └────────────────────────────────────────────────────────────┘        │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ 8. Результаты сохраняются
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    SLURM MASTER (10.36.60.202)                          │
│                  /scratch/$USER/lda_results/                            │
│                  ├── run_cluster_4core_YYYYMMDD_HHMMSS/                 │
│                  │   ├── logs/                                          │
│                  │   ├── experiments/                                   │
│                  │   └── aggregated_results/                            │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ 9. Скачивание результатов
                                    │    scp -r ...
                                    ▼
                    ┌─────────────────────────────┐
                    │  ВАШ ЛОКАЛЬНЫЙ КОМПЬЮТЕР    │
                    │  ./local_results/           │
                    └─────────────────────────────┘
```

---

## Детализация Stage 5: Параллельное выполнение

```
4 PROCESSES (один процесс на алгоритм)
│
├─── Process 1: GA
│    ├─── Thread 1: dataset_1 → 10 runs (seed 42-51)
│    ├─── Thread 2: dataset_2 → 10 runs (seed 42-51)
│    ├─── Thread 3: dataset_3 → 10 runs (seed 42-51)
│    └─── Thread 4: dataset_4 → 10 runs (seed 42-51)
│
├─── Process 2: ES
│    ├─── Thread 1: dataset_1 → 10 runs (seed 42-51)
│    ├─── Thread 2: dataset_2 → 10 runs (seed 42-51)
│    ├─── Thread 3: dataset_3 → 10 runs (seed 42-51)
│    └─── Thread 4: dataset_4 → 10 runs (seed 42-51)
│
├─── Process 3: PABBO_Small
│    ├─── Thread 1: dataset_1 → 10 runs (seed 42-51)
│    ├─── Thread 2: dataset_2 → 10 runs (seed 42-51)
│    ├─── Thread 3: dataset_3 → 10 runs (seed 42-51)
│    └─── Thread 4: dataset_4 → 10 runs (seed 42-51)
│
└─── Process 4: PABBO_Large
     ├─── Thread 1: dataset_1 → 10 runs (seed 42-51)
     ├─── Thread 2: dataset_2 → 10 runs (seed 42-51)
     ├─── Thread 3: dataset_3 → 10 runs (seed 42-51)
     └─── Thread 4: dataset_4 → 10 runs (seed 42-51)

ИТОГО: 4 алгоритма × 4 датасета × 10 runs = 160 экспериментов
```

---

## Временная шкала выполнения (примерная)

```
0h ──────────────────────────────────────────────────────── 24h
│
├─── Stage 1: Train PABBO Small (~30 min)
│
├─── Stage 2: Evaluate PABBO Small (~10 min)
│
├─── Stage 3: Train PABBO Large (~1-2h)
│
├─── Stage 4: Evaluate PABBO Large (~10 min)
│
├─── Stage 5: LDA Experiments (ПАРАЛЛЕЛЬНО, ~18-20h)
│    ├─ GA процесс
│    ├─ ES процесс
│    ├─ PABBO_Small процесс
│    └─ PABBO_Large процесс
│
└─── Stage 6: Aggregate Results (~5 min)
```

---

## Состояния задачи в SLURM

```
sbatch run.sh
     │
     ▼
┌─────────┐
│   PD    │  Pending - задача в очереди, ждет ресурсов
└─────────┘
     │
     ▼
┌─────────┐
│    R    │  Running - задача выполняется
└─────────┘
     │
     ▼
┌─────────┐
│   CG    │  Completing - задача завершается
└─────────┘
     │
     ▼
┌─────────┐
│  DONE   │  Задача завершена (исчезает из squeue)
└─────────┘
```

---

## Мониторинг в реальном времени

```
Terminal 1 (slurm-master):         Terminal 2 (ваш компьютер):
┌────────────────────────────┐     ┌────────────────────────────┐
│ $ tail -f slurm-12345.out  │     │ Браузер:                   │
│                            │     │ http://10.36.60.3:3000/    │
│ [Stage 1] Training...      │     │                            │
│ [Stage 2] Evaluating...    │     │ ┌──────────────────────┐   │
│ [Stage 5] Running exps...  │     │ │   GRAFANA DASHBOARD  │   │
│   - GA: 40/40 complete     │     │ │   ┌──────────────┐   │   │
│   - ES: 38/40 running      │     │ │   │ CPU Usage    │   │   │
│   - PABBO_S: 35/40         │     │ │   │ Memory       │   │   │
│   - PABBO_L: 32/40         │     │ │   │ Network I/O  │   │   │
│ ...                        │     │ │   └──────────────┘   │   │
└────────────────────────────┘     │ └──────────────────────┘   │
                                   └────────────────────────────┘
```

---

## Поток данных

```
INPUT                      PROCESSING                    OUTPUT
───────                    ──────────                    ──────

data/                      for_klaster.py                lda_pipeline_results/
├── X_20news_val_bow.npz   │                            ├── logs/
├── X_nips_val_bow.npz ────┼─► [Training]               │   └── pipeline_main.log
├── X_nytimes_val_bow.npz  │   [Evaluation]             │
└── X_rcv1_val_bow.npz     │   [Optimization] ──────────┼─► experiments/
                           │                            │   ├── 20news/
pabbo_method/              │                            │   ├── nips/
lda_hyperopt/ ─────────────┘                            │   └── ...
                                                        │
                                                        └─► aggregated_results/
                                                            ├── all_results.csv
                                                            ├── statistics.json
                                                            └── *.png
```

---

Эта диаграмма показывает полный жизненный цикл эксперимента от сборки образа до получения результатов! 🎯