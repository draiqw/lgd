"""
Helper package for PABBO transformer models.

This marker file allows importing modules via `pabbo_method.*`
from other projects (e.g. lda_hyperopt).
"""

# Re-export key modules for convenience if needed later.
