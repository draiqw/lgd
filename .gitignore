# --- ОС и IDE ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.code-workspace
.ropeproject

# --- Виртуальные окружения ---
.venv/
venv/
ENV/
env/
.direnv/
.pyenv/
.python-version

# --- Переменные окружения и секреты ---
.env
.env.*
!.env.example
!.env.sample
!.env.template
.flaskenv
.secrets.*
*.key
*.pem

# --- Python байткод и кэш ---
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.cache/
.hypothesis/

# --- Сборка и дистрибутивы ---
build/
dist/
wheels/
share/python-wheels/
*.egg-info/
.eggs/
*.egg
MANIFEST
.installed.cfg

# --- Логи и временные файлы ---
# Игнорируем общие логи, но НЕ результаты экспериментов
pip-log.txt
pip-delete-this-directory.txt

# Игнорируем детальные логи оптимизации (они большие и дублируют информацию)
*_optimization.log
# Игнорируем main.log только в lda_hyperopt (не в pipeline results)
lda_hyperopt/results/main.log

# Игнорируем TensorBoard логи (они огромные)
**/tensorboard/

# --- Тесты и покрытие ---
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache/
coverage.xml
nosetests.xml
*.cover
*.py,cover

# --- Документация и артефакты ---
docs/_build/
doc/_build/

# --- Проекты/фреймворки ---
# Django / Flask / FastAPI (локальные файлы)
db.sqlite3
db.sqlite3-journal
instance/
.webassets-cache
media/

# --- Jupyter ---
.ipynb_checkpoints/

# --- Инструменты/менеджеры пакетов ---
__pypackages__/
# PDM создает каталог с зависимостями
.pdm-python/

# --- Планировщики/очереди ---
celerybeat-schedule
celerybeat.pid

# --- Docker локальные переопределения (опционально) ---
docker-compose.override.yml
.docker/

# --- Node (если фронт рядом с ботом) ---
node_modules/


*.session
*.session-journal

# --- Результаты экспериментов ---
# Игнорируем промежуточные большие результаты, но оставляем финальные

# Игнорируем промежуточные результаты PABBO (могут быть большие модели)
pabbo_method/results/
PABBO/results/

# Игнорируем промежуточные результаты отдельных запусков LDA
lda_hyperopt/results/

# Для результатов pipeline оставляем только агрегированные данные
# Игнорируем отдельные эксперименты (они огромные)
lda_pipeline_results/*/experiments/
# Но оставляем logs и aggregated_results
!lda_pipeline_results/*/logs/
!lda_pipeline_results/*/aggregated_results/
# Игнорируем детальные JSON (они большие)
lda_pipeline_results/*/all_results.json